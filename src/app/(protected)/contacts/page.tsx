// Placeholder Client List screen — the real contact list/detail spec
// (SRS_CHECKLIST.md Section 13) isn't built yet. This exists so the
// sidebar's "Client List" link isn't dead; replace with the real list once
// that spec lands.
export default function ContactsPage() {
  return (
    <main className="mx-auto w-full max-w-5xl px-6 py-12">
      <h1 className="mb-2 text-2xl font-bold tracking-tight">Client List</h1>
      <p className="text-muted-foreground">Coming soon.</p>
    </main>
  );
}
